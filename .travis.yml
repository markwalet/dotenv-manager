language: php

php:
- 7.0
- 7.1
- 7.2

env:
  matrix:
  - PREFER_LOWEST="--prefer-lowest"
  - PREFER_LOWEST=""

before_script:
- composer install --prefer-source --no-interaction $PREFER_LOWEST

script:
- vendor/bin/phpunit

after_success:
- bash <(curl -s https://codecov.io/bash)
